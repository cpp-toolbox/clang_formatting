This repository contains clangd format settings to be used with all opengl-toolbox projects to unify their formatting.

This repository should sit in the root directory, simply run the script which will then create symlinks to these files in the root of your project dir (assumed to be one directory back).
